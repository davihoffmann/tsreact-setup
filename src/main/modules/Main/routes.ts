export const home = {
    exact: true,
    path: '/main',
};

export const pessoa = {
    exact: true,
    path: `${home.path}/pessoa`,
};

export const produto = {
    exact: true,
    path: `${home.path}/produto`,
};

export const nav1 = {
    exact: true,
    path: `${home.path}/nav1`,
};

export const nav2 = {
    exact: true,
    path: `${home.path}/nav2`,
};
